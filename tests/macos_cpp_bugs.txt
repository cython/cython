# C++17 mathematical special functions (std::beta, std::legendre, etc.) are not
# implemented in libc++ (Apple's C++ standard library), so these tests cannot pass
# on macOS with clang regardless of the clang version.
cpp_stl_cmath_cpp17
